import { convertAeoaa } from '../../../helpers/convert-aeoaa';
import { module, test } from 'qunit';

module('Unit | Helper | convert aeoaa');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertAeoaa(42);
  assert.ok(result);
});
