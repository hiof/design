import { toLoverCase } from '../../../helpers/to-lover-case';
import { module, test } from 'qunit';

module('Unit | Helper | to lover case');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toLoverCase(42);
  assert.ok(result);
});
